{
  "name": "irctc-mvp-host",
  "private": true,
  "version": "1.0.0",
  "description": "Frontend-only IRCTC automation MVP host",
  "scripts": {
    "build": "npm --prefix ui install --include=dev && npm --prefix ui run build",
    "start": "node mvp-server.cjs"
  }
}
