{
  "name": "irctc-cypress-automation",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-driven Cypress diagnostics for the IRCTC booking flow.",
  "scripts": {
    "test": "cypress run",
    "test:headed": "cypress run --headed --browser chrome",
    "open": "cypress open",
    "validate": "node --check cypress.config.js && node --check cypress/support/commands.js && node --check cypress/e2e/irctc-entry.cy.js && node --check cypress/utils/index.js"
  },
  "dependencies": {
    "dayjs": "^1.11.13"
  },
  "devDependencies": {
    "cypress": "^13.17.0"
  }
}
